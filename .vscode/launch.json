{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Docker: Attach to Node",
            "port": 9230,
            "sourceMaps": true,
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/app",
            "preLaunchTask": "docker-compose-up"
        }
    ]
}